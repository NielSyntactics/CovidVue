import { createStore } from 'vuex'
import { covid } from "./modules/Covid";

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    covid,
  }
})
